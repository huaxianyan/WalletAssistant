# 更新记录

本项目采用语义化版本号。正式安装包通过 [GitHub Releases](https://github.com/huaxianyan/WalletAssistant/releases) 发布。

## [未发布]

### 发布

- 新增 GitHub Actions 签名发布流程，推送 `vX.Y.Z` Tag 后自动测试、构建、校验签名并创建 Release。

## [1.0.0] - 2026-08-27

首个正式版本。

### 行程导入

- 支持用户自行配置使用 TLS IMAP 和专用密码或授权码的邮箱。
- 首次点击邮箱同步且尚未配置时，说明凭据风险并进入邮箱配置。
- 首次同步读取来自 `12306@rails.com.cn` 的铁路订单通知，后续只读取新增邮件。
- 支持默认关闭的邮箱自动同步，可按 1～24 小时间隔检查新邮件并通知用户确认。
- 支持从服务器文件夹列表中指定同步文件夹，更改后重新建立增量同步游标。
- 手动邮箱同步显示连接、检查、读取邮件和整理行程的实时阶段。
- 支持识别 12306 购票、改签、候补兑现和退票邮件。
- 支持粘贴铁路订单通知正文。
- 支持通过 Android 系统照片选择器导入截图，并使用设备端 ML Kit 中文 OCR 读取文字。
- 支持检查和修正 OCR 文字后再识别行程。
- 解析结果由用户确认后保存，缺失信息不猜测。
- 首次同步或重建索引时可选择仅同步未出发行程，或导入历史行程并直接归档。
- 后续增量同步始终应用购票、改签和退票状态更新。
- 同订单中的不同车次或出发时间拆分为独立行程，同一行程的多位乘车人合并显示。
- 改签后保留原行程为「已改签」，新行程继承对应提醒。
- 部分退票只更新匹配的乘车人和座位，全部退票后行程进入历史记录。
- 相同订单和行程会更新已有记录，删除后可以重新导入。

### 行程管理

- 在设备端保存统一行程模型和完整行程信息。
- 支持查看详情、向左滑动归档、从历史记录恢复和删除行程。
- 支持可选的出发后自动归档，不追溯既往行程。
- 支持跟随系统、浅色和深色主题，并适配 Android 动态配色。

### 乘车提醒

- 新行程提醒默认关闭，可分别控制全局默认值和单个行程。
- 使用 Android 精确提醒，在锁屏和通知栏展示乘车信息。
- Android 16 支持在发车前升级为实时状态。
- 发车前提醒支持 30 分钟～12 小时，默认 3 小时，以 15 分钟为调整粒度。
- 实时状态支持 15～60 分钟，默认 30 分钟，以 5 分钟为调整粒度。
- 通知提供「已上车」和「取消提醒」操作。
- 「已上车」结束提醒并归档，「取消提醒」只结束提醒，不影响独立的自动归档任务。

### Google Wallet

- 支持用户主动将已确认的最少行程信息添加至 Google Wallet Generic Pass。
- Google Wallet Publishing Access 已批准，正式卡片真机添加后不再显示测试标记。
- Wallet 卡片显示路线、车次、出发时间、座位、席别和乘车人。
- 不包含、复制或模拟 12306 动态检票二维码。
- Google Wallet 是可选导出目标，不承担主要提醒职责。

### 隐私与发布

- 邮箱地址和授权码加密保存在设备上，原始邮件、候选邮件、截图和 OCR 文字不发送到开发者运营的服务器。
- 自动同步只持久化待确认的结构化行程和同步游标，不保存原始邮件正文。
- 关闭 Android 应用数据云备份。
- Release 构建启用 R8 代码优化和资源收缩，并移除 Debug 测试入口。
- 正式 APK 使用 NeKo7inA Release 证书签名。
